# Vercel Environment Variables
# Copy these to Vercel Dashboard → Settings → Environment Variables

# Database Connection
# Use either DATABASE_URL (recommended) or individual credentials
# Examples:
#   Vercel Postgres: postgres://default:password@host/verceldb?sslmode=require
#   Railway: postgres://username:password@host/database
#   Supabase: postgresql://postgres:password@host/database
DATABASE_URL=

# Or use individual credentials:
# DB_HOST=
# DB_PORT=5432
# DB_NAME=resort_booking
# DB_USER=
# DB_PASSWORD=

# JWT Configuration
# Generate a strong random string (min 32 characters)
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRES_IN=24h

# Email Configuration (Gmail)
EMAIL_USER=your_gmail@gmail.com
EMAIL_PASS=your_gmail_app_password_here
EMAIL_FROM=your_gmail@gmail.com

# JotForm Integration (Optional)
JOTFORM_WEBHOOK_SECRET=your_webhook_secret_here

# Node Environment
NODE_ENV=production